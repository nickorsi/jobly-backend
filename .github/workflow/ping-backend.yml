name: Ping Backend

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend health endpoint
        run: |
          curl --max-time 90 --fail --silent --show-error \
          https://jobly-backend-nvo.onrender.com